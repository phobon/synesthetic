import { styled } from '~design'

export const Main = styled('main', {
  width: '100%',
  height: '100%',
  display: 'grid',
  placeItems: 'center',
  gridArea: 'main',
})

Main.displayName = 'Main'
